<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_partyon_reports_customer_lines" model="ir.actions.report">
        <field name="name">Presupuesto Líneas Cliente</field>
        <field name="model">sale.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">partyon_reports.customer_lines_report_raw</field>
        <field name="report_file">partyon_reports.customer_lines_report_raw</field>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="binding_type">report</field>
    </record>

    <template id="customer_lines_report_raw">
        <t t-call="web.html_container">
            <t t-foreach="docs" t-as="doc">
                <t t-call="partyon_reports.customer_lines_report" t-lang="doc.partner_id.lang"/>
            </t>
        </t>
    </template>

    <template id="customer_lines_report">
        <t t-call="web.external_layout">
            <t t-set="doc" t-value="doc.with_context(lang=doc.partner_id.lang)"/>
            <t t-set="forced_vat"
               t-value="doc.fiscal_position_id.foreign_vat"/> <!-- So that it appears in the footer of the report instead of the company VAT if it's set -->


            <t t-set="address">
                <div t-field="doc.partner_id" class="mb-0"
                     t-options='{"widget": "contact", "fields": ["address", "name"], "no_marker": True}'/>
                <p t-if="doc.partner_id.vat" class="mb-0">
                    <t t-if="doc.company_id.account_fiscal_country_id.vat_label"
                       t-out="doc.company_id.account_fiscal_country_id.vat_label"/>
                    <t t-else="">Tax ID</t>:
                    <span t-field="doc.partner_id.vat"/>
                </p>
            </t>
            <!--            <t t-set="address" t-value="false"/>-->

            <!--        <div style="position: absolute; top: 0px; right: 0px;width: 300px; min-height: 150px; z-index: 999; text-align: left;">-->
            <!--                <div t-field="doc.partner_id" class="mb-0"-->
            <!--                     t-options='{"widget": "contact", "fields": ["address", "name"], "no_marker": True}'/>-->
            <!--                <p t-if="doc.partner_id.vat" class="mb-0">-->
            <!--                    <t t-if="doc.company_id.account_fiscal_country_id.vat_label"-->
            <!--                       t-out="doc.company_id.account_fiscal_country_id.vat_label"/>-->
            <!--                    <t t-else="">Tax ID</t>:-->
            <!--                    <span t-field="doc.partner_id.vat"/>-->
            <!--                </p>-->
            <!--            </div>-->

            <t t-if="doc.partner_shipping_id == doc.partner_invoice_id
                             and doc.partner_invoice_id != doc.partner_id
                             or doc.partner_shipping_id != doc.partner_invoice_id">
                <t t-set="information_block">
                    <strong>
                        <t t-if="doc.partner_shipping_id == doc.partner_invoice_id">
                            Invoicing and Shipping Address
                        </t>
                        <t t-else="">
                            Invoicing Address
                        </t>
                    </strong>
                    <div t-field="doc.partner_invoice_id"
                         t-options='{"widget": "contact", "fields": ["address", "name", "phone"], "no_marker": True, "phone_icons": True}'/>
                    <t t-if="doc.partner_shipping_id != doc.partner_invoice_id">
                        <strong class="d-block mt-3">Shipping Address</strong>
                        <div t-field="doc.partner_shipping_id"
                             t-options='{"widget": "contact", "fields": ["address", "name", "phone"], "no_marker": True, "phone_icons": True}'/>
                    </t>
                </t>
            </t>
            <div class="page">
                <div class="oe_structure"/>

                <t t-set="is_proforma" t-value="env.context.get('proforma', False) or is_pro_forma"/>
                <t t-set="layout_document_title">
                    <span t-if="is_proforma">Pro-Forma Invoice #</span>
                    <span t-elif="doc.state in ['draft','sent']">Presupuesto #</span>
                    <span t-else="">Orden #</span>
                    <span t-field="doc.name">SO0000</span>
                </t>

                <div class="row mb-4" id="informations">
                    <div t-if="doc.client_order_ref" class="col" name="informations_reference">
                        <strong>Your Reference</strong>
                        <div t-field="doc.client_order_ref">SO0000</div>
                    </div>
                    <div t-if="doc.date_order" class="col" name="informations_date">
                        <strong t-if="is_proforma">Fecha de emisión</strong>
                        <strong t-elif="doc.state in ['draft', 'sent']">Fecha</strong>
                        <strong t-else="">Fecha</strong>
                        <div t-field="doc.date_order" t-options='{"widget": "date"}'>2023-12-31</div>
                    </div>
                    <div t-if="doc.validity_date and doc.state in ['draft', 'sent']"
                         class="col"
                         name="expiration_date">
                        <strong>Vencimiento</strong>
                        <div t-field="doc.validity_date">2023-12-31</div>
                    </div>
                    <div t-if="doc.commitment_date" class="col" name="delivery_date">
                        <strong>Fecha de Entrega</strong>
                        <div
                                t-field="doc.commitment_date"
                                t-options="{'widget': 'date'}"
                        >
                            2023-12-31
                        </div>
                    </div>
                    <div t-if="doc.user_id.name" class="col">
                        <strong>Comercial</strong>
                        <div t-field="doc.user_id">Mitchell Admin</div>
                    </div>
                </div>


                <div class="oe_structure"></div>
                <table class="o_has_total_table table o_main_table table-borderless">
                    <!-- In case we want to repeat the header, remove "display: table-row-group" -->
                    <thead style="display: table-row-group">
                        <tr>
                            <th name="th_product_name" class="text-start">Producto</th>
                            <th name="th_product_amount" cclass="text-end text-nowrap">Cantidad</th>
                            <th name="th_product_price" class="text-start text-nowrap">Precio</th>
                            <th name="th_product_subtotal" class="text-end text-nowrap">Total</th>
                        </tr>
                    </thead>
                    <tbody class="sale_tbody">
                        <tr>
                            <t t-foreach="doc.partyon_sale_line_ids" t-as="line">
                                <tr>
                                    <td>
                                        <span t-out="line.product_name"/>
                                    </td>
                                    <td>
                                        <span t-out="line.product_amount"/>
                                    </td>
                                    <td>
                                        <span t-out="line.price"/>
                                    </td>
                                    <td>
                                        <span t-out="line.line_total"/>
                                    </td>
                                </tr>
                            </t>
                        </tr>
                    </tbody>
                </table>
                <div class="clearfix" name="so_total_summary">
                    <div id="total" class="row mt-n3" name="total">
                        <div t-attf-class="#{'col-6' if report_type != 'html' else 'col-sm-7 col-md-6'} ms-auto">
                            <table class="o_total_table table table-borderless">
                                <!-- Tax totals -->
                                <t t-call="sale.document_tax_totals">
                                    <t t-set="tax_totals" t-value="doc.tax_totals"/>
                                    <t t-set="currency" t-value="doc.currency_id"/>
                                </t>
                            </table>
                        </div>
                    </div>
                </div>
                <div class="oe_structure"></div>

                <div t-if="not doc.signature" class="oe_structure"></div>
                <div t-else="" class="mt-4 ml64 mr4" name="signature">
                    <div class="offset-8">
                        <strong>Signature</strong>
                    </div>
                    <div class="offset-8">
                        <img t-att-src="image_data_uri(doc.signature)" style="max-height: 4cm; max-width: 8cm;"/>
                    </div>
                    <div class="offset-8 text-center">
                        <span t-field="doc.signed_by">Oscar Morgan</span>
                    </div>
                </div>
                <div>
                    <span t-field="doc.note"
                          t-attf-style="#{'text-align:justify;text-justify:inter-word;' if doc.company_id.terms_type != 'html' else ''}"
                          name="order_note"/>
                    <p t-if="not is_html_empty(doc.payment_term_id.note)">
                        <span t-field="doc.payment_term_id.note">The payment should also be transmitted with love</span>
                    </p>
                    <div class="oe_structure"/>
                    <p t-if="doc.fiscal_position_id and not is_html_empty(doc.fiscal_position_id.sudo().note)"
                       id="fiscal_position_remark">
                        <strong>Fiscal Position Remark:</strong>
                        <span t-field="doc.fiscal_position_id.sudo().note">No further requirements for this payment
                        </span>
                    </p>
                </div>
                <div class="oe_structure"/>
                <t t-set="base_address" t-value="doc.env['ir.config_parameter'].sudo().get_param('web.base.url')"/>
                <t t-set="portal_url"
                   t-value="base_address + '/my/orders/' + str(doc.id) + '#portal_connect_software_modal_btn'"/>
                <div t-if="any(u._is_portal() for u in doc.partner_id.user_ids) and doc._get_edi_builders()"
                     class="text-center">
                    <a t-att-href="portal_url">Connect your software</a>
                    with
                    <t t-out="doc.company_id.name"/>
                    to create quotes automatically.
                </div>
            </div>
        </t>
    </template>
</odoo>